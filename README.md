# Neural-Network
A simple two-layer neural network implemented from scratch using NumPy, trained with gradient descent on the make_moons dataset. Includes forward propagation, backpropagation, L2 regularization, and decision boundary visualization.
